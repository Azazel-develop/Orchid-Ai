name: Orchid Build

on:
  push:
    branches:
      - main

jobs:

 build:

  runs-on:
    ubuntu-latest

  steps:

  - uses:
      actions/checkout@v4

  - name:
      Test Orchid

    run:
      python Core/brain.py
